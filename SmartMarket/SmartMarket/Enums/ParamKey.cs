namespace SmartMarket.Enums
{
    public enum ParamKey
    {
        Category,
        CategoryId,
        CategoryName,

        SelectedItem,
        SelectedItemId,
        CoinInWallet,
        TransactionID,
        ContentSearch,

        UserInfo,
        IsEdit,
    }
}
